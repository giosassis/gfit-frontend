import RoutesApp from "./routes";
import Modal from "react-modal";

function App() {
  <Modal
    isOpen={false}
    appElement={document.getElementById("root")} // Defina o elemento raiz da sua aplicação
  ></Modal>;
  return <RoutesApp />;
}

export default App;
